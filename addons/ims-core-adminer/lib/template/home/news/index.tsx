import { observer, inject } from 'mobx-react'
import React = require('react');
@observer
export default class Index extends React.Component<any, any> {
    componentDidMount() { }
    render() {
        return <div className="ims-adminer">新闻</div>
    }
}
